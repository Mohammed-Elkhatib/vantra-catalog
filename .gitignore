# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env.*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# testing / coverage
/coverage

# editor / OS
/.idea
*.swp

# local tooling / agent scratch (not part of the project)
/.playwright-mcp/
/.claude/
/.superpowers/

# stray screenshots / scratch images at repo root
/*.png
/*.jpg

# internal-only, kept local (not part of the public repo).
#   material/             = source catalog/TDS PDFs (also not served; served PDFs live in public/downloads/)
#   research_and_planning/ = internal research, planning, and status notes (see its INDEX.md)
# The data schema these depend on was moved to data/schema.json so the build/test gate stays self-contained.
# Scrubbing the already-committed history of these paths is the deferred fresh-repo/object-storage migration.
/material/
/research_and_planning/
